spring:
  profiles.active: dev
  application.name: BananaMusic
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bananamusicdb
    username: banana
    password: platano
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update

logging:
  level.root: INFO

banana-music.openapi:
  dev-url: http://www.dev.bananamusicdb.es
  prod-url: http://www.prod.bananamusicdb.es

---
spring.profiles: dev

server.port: 9090


---
spring.profiles: prod

server:
  port: 8080
  ssl:
    key-store: keystore2.p12
    key-store-password: abc123.
    keyStoreType: PKCS12
    keyAlias: tomcat

logging:
  level.root: WARN